upstream <%= name %> {
<% if @upstream_cfg_prepend -%><% upstream_cfg_prepend.sort_by{|k,v| k}.each do |key,value| -%>
  <%= key %> <%= value %>;<% end -%><% end -%>
  <% members.each do |i| %>
  server     <%= i %>;<% end %>
}
