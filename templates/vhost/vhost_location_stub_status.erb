  location <%= location %> {
<% if @location_cfg_prepend -%><% location_cfg_prepend.sort_by {|key,value| key}.each do |key,value| -%>
    <%= key %> <%= value %>;
<% end -%><% end -%>
    stub_status on;
<% if @location_cfg_append -%><% location_cfg_append.sort_by {|key,value| key}.each do |key,value| -%>
    <%= key %> <%= value %>;
<% end -%><% end -%>
  }
